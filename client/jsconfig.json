{
  "compilerOptions": {
    "baseUrl": ".", // 경로의 기준을 프로젝트 루트로 설정
    "paths": {
      "@/*": ["src/*"] // @로 시작하는 경로를 src 폴더로 매핑
    }
  },
  "include": [
    "src/**/*" // src 폴더 내의 모든 파일을 포함
  ]
}
